# 子项目 3:战报 UI 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 desktop renderer 内实现原创暗色战报主界面:比分头 / meters / HP 时间轴 / 单位详情 / shuffle 回合导航,fixture 模式支撑浏览器视觉迭代。

**Architecture:** `src/renderer/src/report/{derive,components,data}` 三层——纯函数派生层(严格单测)→ 薄组件(只吃派生结构,d3-scale 算坐标 + 手写 SVG)→ App 重构为 列表侧栏+战报主区+开发者视图。fixture 模式经 `fixtureBridge.ts` 注入 mock `window.gladlog`。Spec:`docs/specs/2026-07-10-report-ui-design.md`。

**Tech Stack:** React 19、TypeScript strict、vitest(globals;组件测试 jsdom + @testing-library/react)、d3-scale、手写 SVG。

## Global Constraints

- **合规(硬性)**:实现者不得读取 `/Users/mingjianliu/code/wowarenalogs` 下任何文件(fixture 生成任务除外,该任务只允许读 `GLADLOG_FIXTURES` 指向的 .txt 日志文件,不许读任何源码)。视觉与布局为原创,不参考旧版。
- 金额口径:**原生模型为正数**(已实证:l3.collect.test.ts 断言 damage effectiveAmount=100、heal=150),派生层直接求和,禁止引入 compat 的取负惯例。
- ESM、TS strict、vitest globals;测试放 `packages/desktop/test/`;根命令 `npm test --workspaces --if-present` 始终全绿。
- 派生函数为纯函数、不 import React/Electron/DOM;组件不直接遍历 GladMatch 原始事件数组。
- 职业色/名称与 spec 名称为暴雪公开事实硬编码,文件头注明"子项目 5 后由管线产物替换"。
- TDD、每任务一 commit(conventional commits)。
- 新 npm 依赖仅限:`d3-scale`、`@types/d3-scale`、`jsdom`、`@testing-library/react`(全 MIT)。

## 文件结构总览

```
packages/desktop/
  scripts/make-report-fixture.mjs        # Task 1:日志→脱敏对局 JSON
  test/fixtures/report-match.json        # Task 1:入库的脱敏 3v3 GladMatch(data 部分)
  test/fixtures/loadFixture.ts           # Task 1:加载 helper + buildSyntheticShuffle
  src/renderer/src/report/derive/types.ts     # Task 2
  src/renderer/src/report/derive/roster.ts    # Task 2
  src/renderer/src/report/derive/summary.ts   # Task 3
  src/renderer/src/report/derive/timeline.ts  # Task 4
  src/renderer/src/report/derive/casts.ts     # Task 5
  src/renderer/src/report/data/gameConstants.ts # Task 6
  src/renderer/src/report/components/ReportHeader.tsx  Meters.tsx   # Task 7
  src/renderer/src/report/components/Timeline.tsx  UnitPanel.tsx    # Task 8
  src/renderer/src/report/components/MatchReport.tsx  ShuffleReport.tsx  # Task 9
  src/renderer/src/fixtureBridge.ts      # Task 9
  src/renderer/src/components/DevPanel.tsx   # Task 9(现调试页迁入)
  src/renderer/src/App.tsx               # Task 9 重构
  src/renderer/src/styles.css            # Task 7-10 追加
  test/report.*.test.ts(x)               # 各任务
```

---

### Task 1: 脱敏 fixture 资产 + 加载 helper

**Files:**

- Create: `packages/desktop/scripts/make-report-fixture.mjs`
- Create: `packages/desktop/test/fixtures/report-match.json`(脚本产物,入库)
- Create: `packages/desktop/test/fixtures/loadFixture.ts`

**Interfaces:**

- Produces:

```ts
// test/fixtures/loadFixture.ts
import type {
  StoredMatch,
  StoredShuffle,
} from "../../src/renderer/src/report/derive/types";
export function loadMatchFixture(): StoredMatch; // 读 report-match.json
export function buildSyntheticShuffle(base: StoredMatch): StoredShuffle;
// rounds = 3 份 base 的浅拷贝改造:kind:'shuffleRound', sequenceNumber 0/1/2,
// startTime/endTime 依次偏移 +i*90_000,winningTeamId 交替 0/1/0;
// shuffle 顶层 { kind:'shuffle', rounds, startTime=rounds[0].startTime, endTime=rounds[2].endTime, result: base.result }
```

注意:Task 2 才创建 `types.ts`;本任务的 `loadFixture.ts` 先用 `any` 返回并留 TODO 类型引用?——**不行(No Placeholders)**。顺序调整:本任务只产 `make-report-fixture.mjs` + `report-match.json`;`loadFixture.ts` 归入 Task 2(那里有类型)。

- [ ] **Step 1: 写生成脚本**

```js
// packages/desktop/scripts/make-report-fixture.mjs
// 用法(repo 根): node_modules/.bin/tsx packages/desktop/scripts/make-report-fixture.mjs \
//   --log "$GLADLOG_FIXTURES/3v3_tww_1120_reduced.txt" --out packages/desktop/test/fixtures/report-match.json
// 读取真实日志→GladLogParser→取第一个 match 事件→剥 rawLines→玩家名脱敏→写 JSON。
import { readFileSync, writeFileSync } from "fs";
import { GladLogParser } from "../../parser/src/index.ts";

const arg = (name) => {
  const i = process.argv.indexOf(`--${name}`);
  return i === -1 ? null : process.argv[i + 1];
};
const logPath = arg("log");
const outPath = arg("out");
if (!logPath || !outPath) {
  console.error(
    "usage: tsx make-report-fixture.mjs --log <combatlog.txt> --out <fixture.json>",
  );
  process.exit(1);
}

const parser = new GladLogParser({ timezone: "UTC" });
let match = null;
parser.on("match", (m) => {
  if (!match) match = m;
});
for (const line of readFileSync(logPath, "utf-8").split("\n"))
  parser.push(line);
parser.end();
if (!match) {
  console.error("no match produced from log");
  process.exit(1);
}

const { rawLines, ...data } = match;
let text = JSON.stringify(data, null, 1);
// 脱敏:每个 Player 单位的名字(Name-Realm)全局替换为 PlayerA-Test 等
const players = Object.values(data.units).filter((u) => u.kind === "Player");
players.forEach((u, i) => {
  const alias = `Player${String.fromCharCode(65 + i)}-Test`;
  text = text.split(JSON.stringify(u.name).slice(1, -1)).join(alias);
});
writeFileSync(outPath, text);
console.log(
  `wrote ${outPath}: ${players.length} players sanitized, ${text.length} bytes`,
);
```

- [ ] **Step 2: 生成 fixture 并抽查**

Run(repo 根,需环境变量): `GLADLOG_FIXTURES=/Users/mingjianliu/code/wowarenalogs/packages/parser/test/testlogs node_modules/.bin/tsx packages/desktop/scripts/make-report-fixture.mjs --log "$GLADLOG_FIXTURES/3v3_tww_1120_reduced.txt" --out packages/desktop/test/fixtures/report-match.json`
Expected: `wrote ... 6 players sanitized`;抽查 JSON:`grep -c "Player[A-F]-Test" ...` > 0 且 `grep -c "拉格纳罗斯\|原始玩家名片段"` = 0(用 `python3 -c "import json;d=json.load(open('packages/desktop/test/fixtures/report-match.json'));print(d['kind'],d['bracket'],len(d['units']))"` 确认 kind=match、bracket=3v3)。文件 <200KB(reduced 日志)。

- [ ] **Step 3: Commit**

```bash
git add packages/desktop/scripts/make-report-fixture.mjs packages/desktop/test/fixtures/report-match.json
git commit -m "feat(desktop): sanitized report fixture + generator script"
```

---

### Task 2: derive/types + roster + fixture 加载 helper

**Files:**

- Create: `packages/desktop/src/renderer/src/report/derive/types.ts`、`roster.ts`
- Create: `packages/desktop/test/fixtures/loadFixture.ts`
- Test: `packages/desktop/test/report.roster.test.ts`

**Interfaces:**

- Consumes: `GladMatch`/`GladShuffle`/`GladShuffleRound`(`@gladlog/parser`)、`report-match.json`(Task 1)
- Produces(后续所有任务的公共类型):

```ts
// derive/types.ts —— 完整文件
import type { GladMatch, GladShuffle, GladShuffleRound } from "@gladlog/parser";

export type StoredMatch = Omit<GladMatch, "rawLines">;
export type StoredShuffleRound = Omit<GladShuffleRound, "rawLines">;
export type StoredShuffle = Omit<GladShuffle, "rawLines" | "rounds"> & {
  rounds: StoredShuffleRound[];
};
/** 单场战报的输入:普通对局或 shuffle 单回合(同构) */
export type ReportSource = StoredMatch | StoredShuffleRound;
```

```ts
// derive/roster.ts
export interface RosterPlayer {
  unitId: string;
  name: string;
  classId: number;
  specId: number;
  teamId: number;
  rating: number | null;
  isLogOwner: boolean;
}
export interface RosterTeam {
  teamId: number;
  players: RosterPlayer[];
  isWinner: boolean;
  isPlayerTeam: boolean;
}
export function deriveRoster(m: ReportSource): RosterTeam[];
// 规则:units 中 kind==='Player' 且有 info 的单位;teamId 取 info.teamId;rating 取 info.personalRating(0→null 不判,原样保留数值,仅 info 缺失→null);
// isLogOwner = id===m.playerId;isWinner = teamId===m.winningTeamId;isPlayerTeam = teamId===m.playerTeamId;
// 每队按 name 升序;返回数组玩家队在前(playerTeamId null 时按 teamId 升序)。
```

```ts
// test/fixtures/loadFixture.ts —— 完整文件
import { readFileSync } from "fs";
import { join } from "path";
import type {
  StoredMatch,
  StoredShuffle,
  StoredShuffleRound,
} from "../../src/renderer/src/report/derive/types";

export function loadMatchFixture(): StoredMatch {
  return JSON.parse(
    readFileSync(join(__dirname, "fixtures", "report-match.json"), "utf-8"),
  ) as StoredMatch;
}

export function buildSyntheticShuffle(base: StoredMatch): StoredShuffle {
  const rounds: StoredShuffleRound[] = [0, 1, 2].map((i) => ({
    ...base,
    kind: "shuffleRound" as const,
    sequenceNumber: i,
    startTime: base.startTime + i * 90_000,
    endTime: base.endTime + i * 90_000,
    winningTeamId: i % 2,
  }));
  return {
    kind: "shuffle",
    rounds,
    startTime: rounds[0]!.startTime,
    endTime: rounds[2]!.endTime,
    result: base.result,
  };
}
```

注:`__dirname` 在 ESM vitest 下不可用——改用 `new URL("fixtures/report-match.json", import.meta.url).pathname`。实现按此写。

- [ ] **Step 1: 写失败测试**

```ts
// packages/desktop/test/report.roster.test.ts
import { deriveRoster } from "../src/renderer/src/report/derive/roster";
import { loadMatchFixture } from "./fixtures/loadFixture";

describe("deriveRoster", () => {
  const m = loadMatchFixture();
  it("两队、每队 3 名 Player、玩家队在前", () => {
    const teams = deriveRoster(m);
    expect(teams).toHaveLength(2);
    expect(teams[0]!.players).toHaveLength(3);
    expect(teams[1]!.players).toHaveLength(3);
    expect(teams[0]!.isPlayerTeam).toBe(true);
  });
  it("胜负标注与 winningTeamId 一致;log owner 恰好一人", () => {
    const teams = deriveRoster(m);
    const winners = teams.filter((t) => t.isWinner);
    expect(winners).toHaveLength(1);
    expect(winners[0]!.teamId).toBe(m.winningTeamId);
    const owners = teams.flatMap((t) => t.players).filter((p) => p.isLogOwner);
    expect(owners).toHaveLength(1);
    expect(owners[0]!.unitId).toBe(m.playerId);
  });
  it("队内按名字升序;rating 来自 CombatantInfo", () => {
    const teams = deriveRoster(m);
    for (const t of teams) {
      const names = t.players.map((p) => p.name);
      expect(names).toEqual([...names].sort());
    }
    expect(
      teams
        .flatMap((t) => t.players)
        .every((p) => p.rating === null || typeof p.rating === "number"),
    ).toBe(true);
  });
});
```

- [ ] **Step 2: 确认失败** — Run(cwd `packages/desktop`): `npx vitest run test/report.roster.test.ts`,Expected: FAIL(模块缺失)

- [ ] **Step 3: 实现**

```ts
// src/renderer/src/report/derive/roster.ts —— 完整文件
import type { ReportSource } from "./types";

export interface RosterPlayer {
  unitId: string;
  name: string;
  classId: number;
  specId: number;
  teamId: number;
  rating: number | null;
  isLogOwner: boolean;
}
export interface RosterTeam {
  teamId: number;
  players: RosterPlayer[];
  isWinner: boolean;
  isPlayerTeam: boolean;
}

export function deriveRoster(m: ReportSource): RosterTeam[] {
  const byTeam = new Map<number, RosterPlayer[]>();
  for (const u of Object.values(m.units)) {
    if (u.kind !== "Player" || !u.info) continue;
    const p: RosterPlayer = {
      unitId: u.id,
      name: u.name,
      classId: u.classId,
      specId: u.specId,
      teamId: u.info.teamId,
      rating: u.info ? u.info.personalRating : null,
      isLogOwner: u.id === m.playerId,
    };
    const list = byTeam.get(p.teamId) ?? [];
    list.push(p);
    byTeam.set(p.teamId, list);
  }
  const teams = [...byTeam.entries()].map(([teamId, players]) => ({
    teamId,
    players: players.sort((a, b) => a.name.localeCompare(b.name)),
    isWinner: teamId === m.winningTeamId,
    isPlayerTeam: teamId === m.playerTeamId,
  }));
  teams.sort(
    (a, b) =>
      Number(b.isPlayerTeam) - Number(a.isPlayerTeam) || a.teamId - b.teamId,
  );
  return teams;
}
```

`types.ts` 与 `loadFixture.ts` 按 Interfaces 全文创建(loadFixture 用 `new URL(..., import.meta.url).pathname`)。

- [ ] **Step 4: 验证** — Run(repo 根): `npm test -w @gladlog/desktop && npm run typecheck -w @gladlog/desktop`,Expected: PASS
- [ ] **Step 5: Commit**

```bash
git add packages/desktop/src/renderer/src/report/derive packages/desktop/test/fixtures/loadFixture.ts packages/desktop/test/report.roster.test.ts
git commit -m "feat(desktop): report derive types + roster derivation"
```

---

### Task 3: derive/summary(单位聚合,宠物并入)

**Files:**

- Create: `packages/desktop/src/renderer/src/report/derive/summary.ts`
- Test: `packages/desktop/test/report.summary.test.ts`

**Interfaces:**

- Consumes: `ReportSource`(Task 2)
- Produces:

```ts
export interface UnitTotals {
  unitId: string;
  name: string;
  classId: number;
  specId: number;
  teamId: number;
  damageDone: number; // Σ 本人+宠物 damageOut.effectiveAmount
  healingDone: number; // Σ 本人+宠物 healOut.effectiveAmount
  absorbsDone: number; // Σ absorbsOut.absorbedAmount
  damageTaken: number; // Σ 本人 damageIn.effectiveAmount
  deaths: number; // deaths 中 unconscious===false 的条数
  dps: number;
  hps: number; // done / durationSec,duration=(endTime-startTime)/1000,<=0 时记 0
}
export function deriveSummary(m: ReportSource): UnitTotals[]; // 每 Player(有 info)一行,按 damageDone 降序
```

- [ ] **Step 1: 写失败测试**(合成对局精确断言 + fixture 守恒断言)

```ts
// packages/desktop/test/report.summary.test.ts
import { deriveSummary } from "../src/renderer/src/report/derive/summary";
import type { ReportSource } from "../src/renderer/src/report/derive/types";
import { loadMatchFixture } from "./fixtures/loadFixture";

function hp(effective: number): {
  timestamp: number;
  eventName: string;
  spellId: number;
  spellName: string;
  srcId: string;
  srcName: string;
  destId: string;
  destName: string;
  params: string[];
  amount: number;
  effectiveAmount: number;
} {
  return {
    timestamp: 1000,
    eventName: "SPELL_DAMAGE",
    spellId: 1,
    spellName: "X",
    srcId: "s",
    srcName: "S",
    destId: "d",
    destName: "D",
    params: [],
    amount: effective,
    effectiveAmount: effective,
  };
}
const emptyUnit = {
  ownerId: undefined,
  reaction: "Hostile" as const,
  damageOut: [],
  damageIn: [],
  healOut: [],
  healIn: [],
  absorbsOut: [],
  absorbsIn: [],
  casts: [],
  petCasts: [],
  auraEvents: [],
  actionsOut: [],
  actionsIn: [],
  deaths: [],
  unconsciousEvents: [],
  advancedSamples: [],
};
function synthetic(): ReportSource {
  return {
    kind: "match",
    id: "syn",
    bracket: "2v2",
    zoneId: "1",
    isRated: true,
    startTime: 0,
    endTime: 10_000,
    playerId: "P1",
    playerTeamId: 0,
    winningTeamId: 0,
    result: "Win",
    linesTotal: 0,
    linesDropped: 0,
    hasAdvancedLogging: true,
    timezone: "UTC",
    units: {
      P1: {
        ...emptyUnit,
        id: "P1",
        name: "A-T",
        kind: "Player",
        classId: 1,
        specId: 71,
        info: {
          teamId: 0,
          specId: 71,
          personalRating: 1800,
          talents: [],
          pvpTalents: [],
          equipment: [],
          interestingAuras: [],
        },
        damageOut: [hp(100), hp(250)],
        healOut: [hp(30)],
        damageIn: [hp(500)],
        deaths: [
          { ...hp(0), unconscious: false },
          { ...hp(0), unconscious: true },
        ],
      },
      PET1: {
        ...emptyUnit,
        id: "PET1",
        name: "Pet",
        kind: "Pet",
        classId: 0,
        specId: 0,
        ownerId: "P1",
        damageOut: [hp(50)],
        healOut: [hp(20)],
      },
      P2: {
        ...emptyUnit,
        id: "P2",
        name: "B-T",
        kind: "Player",
        classId: 2,
        specId: 65,
        info: {
          teamId: 1,
          specId: 65,
          personalRating: 1900,
          talents: [],
          pvpTalents: [],
          equipment: [],
          interestingAuras: [],
        },
        absorbsOut: [{ ...hp(0), absorbedAmount: 400, attackerId: "P1" }],
      },
    },
  } as unknown as ReportSource;
}

describe("deriveSummary", () => {
  it("合成对局:宠物并入、口径精确、排序按 damageDone 降序", () => {
    const rows = deriveSummary(synthetic());
    expect(rows).toHaveLength(2); // 宠物不单列
    expect(rows[0]!.unitId).toBe("P1");
    expect(rows[0]!.damageDone).toBe(400); // 100+250+宠物50
    expect(rows[0]!.healingDone).toBe(50); // 30+宠物20
    expect(rows[0]!.damageTaken).toBe(500);
    expect(rows[0]!.deaths).toBe(1); // unconscious 不计
    expect(rows[0]!.dps).toBeCloseTo(40); // 400/10s
    expect(rows[1]!.absorbsDone).toBe(400);
  });
  it("fixture 守恒:每行数值非负,行数=Player 数", () => {
    const m = loadMatchFixture();
    const rows = deriveSummary(m);
    const players = Object.values(m.units).filter(
      (u) => u.kind === "Player" && u.info,
    );
    expect(rows).toHaveLength(players.length);
    for (const r of rows) {
      expect(r.damageDone).toBeGreaterThanOrEqual(0);
      expect(r.healingDone).toBeGreaterThanOrEqual(0);
      expect(r.damageTaken).toBeGreaterThanOrEqual(0);
    }
    expect(rows.some((r) => r.damageDone > 0)).toBe(true);
  });
});
```

- [ ] **Step 2: 确认失败** — Run: `npx vitest run test/report.summary.test.ts`,Expected: FAIL

- [ ] **Step 3: 实现**

```ts
// src/renderer/src/report/derive/summary.ts —— 完整文件
import type { ReportSource } from "./types";

export interface UnitTotals {
  unitId: string;
  name: string;
  classId: number;
  specId: number;
  teamId: number;
  damageDone: number;
  healingDone: number;
  absorbsDone: number;
  damageTaken: number;
  deaths: number;
  dps: number;
  hps: number;
}

const sum = (events: { effectiveAmount: number }[]): number =>
  events.reduce((acc, e) => acc + e.effectiveAmount, 0);

export function deriveSummary(m: ReportSource): UnitTotals[] {
  const units = Object.values(m.units);
  const durationSec = (m.endTime - m.startTime) / 1000;
  const rows: UnitTotals[] = [];
  for (const u of units) {
    if (u.kind !== "Player" || !u.info) continue;
    const pets = units.filter((p) => p.ownerId === u.id);
    const damageDone =
      sum(u.damageOut) + pets.reduce((a, p) => a + sum(p.damageOut), 0);
    const healingDone =
      sum(u.healOut) + pets.reduce((a, p) => a + sum(p.healOut), 0);
    const absorbsDone = u.absorbsOut.reduce((a, e) => a + e.absorbedAmount, 0);
    const damageTaken = sum(u.damageIn);
    const deaths = u.deaths.filter((d) => !d.unconscious).length;
    rows.push({
      unitId: u.id,
      name: u.name,
      classId: u.classId,
      specId: u.specId,
      teamId: u.info.teamId,
      damageDone,
      healingDone,
      absorbsDone,
      damageTaken,
      deaths,
      dps: durationSec > 0 ? damageDone / durationSec : 0,
      hps: durationSec > 0 ? healingDone / durationSec : 0,
    });
  }
  return rows.sort((a, b) => b.damageDone - a.damageDone);
}
```

- [ ] **Step 4: 验证** — Run: `npm test -w @gladlog/desktop && npm run typecheck -w @gladlog/desktop`,Expected: PASS
- [ ] **Step 5: Commit**

```bash
git add packages/desktop/src/renderer/src/report/derive/summary.ts packages/desktop/test/report.summary.test.ts
git commit -m "feat(desktop): unit summary derivation with pet merge"
```

---

### Task 4: derive/timeline(HP 序列 + 死亡标记)

**Files:**

- Create: `packages/desktop/src/renderer/src/report/derive/timeline.ts`
- Test: `packages/desktop/test/report.timeline.test.ts`

**Interfaces:**

- Consumes: `ReportSource`
- Produces:

```ts
export interface HpPoint {
  t: number;
  hp: number;
  maxHp: number;
}
export interface DeathMark {
  t: number;
  unitId: string;
  name: string;
}
export interface TimelineData {
  start: number;
  end: number;
  hasAdvanced: boolean;
  series: {
    unitId: string;
    name: string;
    classId: number;
    teamId: number;
    points: HpPoint[];
  }[]; // 仅 Player(有 info),按 teamId 再 name 排序
  deaths: DeathMark[]; // unconscious===false,按 t 升序
}
export function deriveTimeline(m: ReportSource): TimelineData;
// hasAdvanced=m.hasAdvancedLogging;false 或采样为空 → series=[](组件退化为仅标记);
// points 按 t 升序;t/start/end 均为绝对 epoch ms(组件层再做归一)。
```

- [ ] **Step 1: 写失败测试**

```ts
// packages/desktop/test/report.timeline.test.ts
import { deriveTimeline } from "../src/renderer/src/report/derive/timeline";
import { loadMatchFixture } from "./fixtures/loadFixture";

describe("deriveTimeline", () => {
  const m = loadMatchFixture();
  it("fixture(advanced 日志):每个 Player 一条序列,点按时间升序", () => {
    const t = deriveTimeline(m);
    expect(t.hasAdvanced).toBe(true);
    expect(t.series.length).toBeGreaterThan(0);
    for (const s of t.series) {
      for (let i = 1; i < s.points.length; i++)
        expect(s.points[i]!.t).toBeGreaterThanOrEqual(s.points[i - 1]!.t);
      for (const p of s.points) expect(p.maxHp).toBeGreaterThan(0);
    }
  });
  it("死亡标记数量=非假死 deaths 总数,时间在对局范围内", () => {
    const t = deriveTimeline(m);
    const expected = Object.values(m.units)
      .filter((u) => u.kind === "Player")
      .reduce((a, u) => a + u.deaths.filter((d) => !d.unconscious).length, 0);
    expect(t.deaths).toHaveLength(expected);
    for (const d of t.deaths) {
      expect(d.t).toBeGreaterThanOrEqual(t.start);
      expect(d.t).toBeLessThanOrEqual(t.end);
    }
  });
  it("hasAdvanced=false → series 空", () => {
    const noAdv = { ...m, hasAdvancedLogging: false };
    expect(deriveTimeline(noAdv).series).toEqual([]);
  });
});
```

- [ ] **Step 2: 确认失败** — Run: `npx vitest run test/report.timeline.test.ts`,Expected: FAIL

- [ ] **Step 3: 实现**

```ts
// src/renderer/src/report/derive/timeline.ts —— 完整文件
import type { ReportSource } from "./types";

export interface HpPoint {
  t: number;
  hp: number;
  maxHp: number;
}
export interface DeathMark {
  t: number;
  unitId: string;
  name: string;
}
export interface TimelineData {
  start: number;
  end: number;
  hasAdvanced: boolean;
  series: {
    unitId: string;
    name: string;
    classId: number;
    teamId: number;
    points: HpPoint[];
  }[];
  deaths: DeathMark[];
}

export function deriveTimeline(m: ReportSource): TimelineData {
  const players = Object.values(m.units).filter(
    (u) => u.kind === "Player" && u.info,
  );
  const series = !m.hasAdvancedLogging
    ? []
    : players
        .map((u) => ({
          unitId: u.id,
          name: u.name,
          classId: u.classId,
          teamId: u.info!.teamId,
          points: [...u.advancedSamples]
            .sort((a, b) => a.timestamp - b.timestamp)
            .map((s) => ({ t: s.timestamp, hp: s.hp, maxHp: s.maxHp })),
        }))
        .filter((s) => s.points.length > 0)
        .sort((a, b) => a.teamId - b.teamId || a.name.localeCompare(b.name));
  const deaths: DeathMark[] = players
    .flatMap((u) =>
      u.deaths
        .filter((d) => !d.unconscious)
        .map((d) => ({ t: d.timestamp, unitId: u.id, name: u.name })),
    )
    .sort((a, b) => a.t - b.t);
  return {
    start: m.startTime,
    end: m.endTime,
    hasAdvanced: m.hasAdvancedLogging,
    series,
    deaths,
  };
}
```

- [ ] **Step 4: 验证** — Run: `npm test -w @gladlog/desktop && npm run typecheck -w @gladlog/desktop`,Expected: PASS
- [ ] **Step 5: Commit**

```bash
git add packages/desktop/src/renderer/src/report/derive/timeline.ts packages/desktop/test/report.timeline.test.ts
git commit -m "feat(desktop): timeline derivation — hp series + death marks"
```

---

### Task 5: derive/casts(施法与光环序列)

**Files:**

- Create: `packages/desktop/src/renderer/src/report/derive/casts.ts`
- Test: `packages/desktop/test/report.casts.test.ts`

**Interfaces:**

- Consumes: `ReportSource`
- Produces:

```ts
export interface CastRow {
  t: number;
  spellId: number;
  spellName: string;
  targetName: string;
  byPet: boolean;
}
export interface AuraRow {
  t: number;
  spellId: number;
  spellName: string;
  auraType: "BUFF" | "DEBUFF";
  applied: boolean;
}
export function deriveCasts(m: ReportSource, unitId: string): CastRow[];
// = units[unitId].casts(byPet:false)+petCasts(byPet:true) 合并按 t 升序;targetName=destName(空串保留)
export function deriveAuraEvents(m: ReportSource, unitId: string): AuraRow[];
// = auraEvents 按 t 升序;applied = !eventName.includes("REMOVED")
// unitId 不存在 → 两函数均返回 []
```

- [ ] **Step 1: 写失败测试**

```ts
// packages/desktop/test/report.casts.test.ts
import {
  deriveAuraEvents,
  deriveCasts,
} from "../src/renderer/src/report/derive/casts";
import { loadMatchFixture } from "./fixtures/loadFixture";

describe("deriveCasts / deriveAuraEvents", () => {
  const m = loadMatchFixture();
  const anyPlayer = Object.values(m.units).find(
    (u) => u.kind === "Player" && u.casts.length > 0,
  )!;
  it("施法序列升序合并,数量=casts+petCasts", () => {
    const rows = deriveCasts(m, anyPlayer.id);
    expect(rows).toHaveLength(
      anyPlayer.casts.length + anyPlayer.petCasts.length,
    );
    for (let i = 1; i < rows.length; i++)
      expect(rows[i]!.t).toBeGreaterThanOrEqual(rows[i - 1]!.t);
    expect(rows.every((r) => r.spellName.length > 0)).toBe(true);
  });
  it("光环序列:applied 标记与 REMOVED 事件名互补", () => {
    const withAuras = Object.values(m.units).find(
      (u) => u.kind === "Player" && u.auraEvents.length > 0,
    )!;
    const rows = deriveAuraEvents(m, withAuras.id);
    expect(rows).toHaveLength(withAuras.auraEvents.length);
    const removed = withAuras.auraEvents.filter((e) =>
      e.eventName.includes("REMOVED"),
    ).length;
    expect(rows.filter((r) => !r.applied)).toHaveLength(removed);
  });
  it("未知 unitId → 空数组", () => {
    expect(deriveCasts(m, "nope")).toEqual([]);
    expect(deriveAuraEvents(m, "nope")).toEqual([]);
  });
});
```

- [ ] **Step 2: 确认失败** — Run: `npx vitest run test/report.casts.test.ts`,Expected: FAIL

- [ ] **Step 3: 实现**

```ts
// src/renderer/src/report/derive/casts.ts —— 完整文件
import type { ReportSource } from "./types";

export interface CastRow {
  t: number;
  spellId: number;
  spellName: string;
  targetName: string;
  byPet: boolean;
}
export interface AuraRow {
  t: number;
  spellId: number;
  spellName: string;
  auraType: "BUFF" | "DEBUFF";
  applied: boolean;
}

export function deriveCasts(m: ReportSource, unitId: string): CastRow[] {
  const u = m.units[unitId];
  if (!u) return [];
  const row =
    (byPet: boolean) =>
    (e: (typeof u.casts)[number]): CastRow => ({
      t: e.timestamp,
      spellId: e.spellId,
      spellName: e.spellName,
      targetName: e.destName,
      byPet,
    });
  return [...u.casts.map(row(false)), ...u.petCasts.map(row(true))].sort(
    (a, b) => a.t - b.t,
  );
}

export function deriveAuraEvents(m: ReportSource, unitId: string): AuraRow[] {
  const u = m.units[unitId];
  if (!u) return [];
  return [...u.auraEvents]
    .sort((a, b) => a.timestamp - b.timestamp)
    .map((e) => ({
      t: e.timestamp,
      spellId: e.spellId,
      spellName: e.spellName,
      auraType: e.auraType,
      applied: !e.eventName.includes("REMOVED"),
    }));
}
```

- [ ] **Step 4: 验证** — Run: `npm test -w @gladlog/desktop && npm run typecheck -w @gladlog/desktop`,Expected: PASS
- [ ] **Step 5: Commit**

```bash
git add packages/desktop/src/renderer/src/report/derive/casts.ts packages/desktop/test/report.casts.test.ts
git commit -m "feat(desktop): cast and aura sequence derivation"
```

---

### Task 6: data/gameConstants(职业色/名称、spec 名)

**Files:**

- Create: `packages/desktop/src/renderer/src/report/data/gameConstants.ts`
- Test: `packages/desktop/test/report.gameConstants.test.ts`

**Interfaces:**

- Produces:

```ts
export const CLASS_COLORS: Record<number, string>;
export const CLASS_NAMES: Record<number, string>;
export const SPEC_NAMES: Record<number, string>;
export function classColor(classId: number): string; // 未知 → "#9d9d9d"
export function className(classId: number): string; // 未知 → "Unknown"
export function specName(specId: number): string; // 未知 → ""
```

内容为暴雪公开事实(文件头注释:「来源:暴雪官方 UI 色板与 specialization ID 公开文档;子项目 5 数据管线建成后由生成产物替换」)。职业 1..13:Warrior #C69B6D、Paladin #F48CBA、Hunter #AAD372、Rogue #FFF468、Priest #FFFFFF、Death Knight #C41E3A、Shaman #0070DD、Mage #3FC7EB、Warlock #8788EE、Monk #00FF98、Druid #FF7C0A、Demon Hunter #A330C9、Evoker #33937F。SPEC_NAMES 覆盖全部 40 个玩家 spec(62 Arcane Mage、63 Fire Mage、64 Frost Mage、65 Holy Paladin、66 Protection Paladin、70 Retribution Paladin、71 Arms Warrior、72 Fury Warrior、73 Protection Warrior、102 Balance Druid、103 Feral Druid、104 Guardian Druid、105 Restoration Druid、250 Blood DK、251 Frost DK、252 Unholy DK、253 Beast Mastery Hunter、254 Marksmanship Hunter、255 Survival Hunter、256 Discipline Priest、257 Holy Priest、258 Shadow Priest、259 Assassination Rogue、260 Outlaw Rogue、261 Subtlety Rogue、262 Elemental Shaman、263 Enhancement Shaman、264 Restoration Shaman、265 Affliction Warlock、266 Demonology Warlock、267 Destruction Warlock、268 Brewmaster Monk、269 Windwalker Monk、270 Mistweaver Monk、577 Havoc DH、581 Vengeance DH、1467 Devastation Evoker、1468 Preservation Evoker、1473 Augmentation Evoker)。

- [ ] **Step 1: 写失败测试**

```ts
// packages/desktop/test/report.gameConstants.test.ts
import {
  CLASS_COLORS,
  classColor,
  className,
  SPEC_NAMES,
  specName,
} from "../src/renderer/src/report/data/gameConstants";
import { loadMatchFixture } from "./fixtures/loadFixture";

describe("gameConstants", () => {
  it("13 个职业都有色与名;未知回退", () => {
    for (let c = 1; c <= 13; c++) {
      expect(CLASS_COLORS[c]).toMatch(/^#[0-9A-Fa-f]{6}$/);
      expect(className(c).length).toBeGreaterThan(0);
    }
    expect(classColor(999)).toBe("#9d9d9d");
    expect(className(999)).toBe("Unknown");
    expect(specName(999999)).toBe("");
  });
  it("fixture 中出现的每个 classId/specId 都被覆盖", () => {
    const m = loadMatchFixture();
    for (const u of Object.values(m.units)) {
      if (u.kind !== "Player") continue;
      expect(CLASS_COLORS[u.classId], `classId ${u.classId}`).toBeDefined();
      expect(SPEC_NAMES[u.specId], `specId ${u.specId}`).toBeDefined();
    }
  });
});
```

- [ ] **Step 2: 确认失败** — Run: `npx vitest run test/report.gameConstants.test.ts`,Expected: FAIL
- [ ] **Step 3: 实现** — 按 Interfaces 的完整清单写 `gameConstants.ts`(三个 Record 字面量 + 三个带回退的取值函数;色值/名称照抄上文)。
- [ ] **Step 4: 验证** — Run: `npm test -w @gladlog/desktop && npm run typecheck -w @gladlog/desktop`,Expected: PASS
- [ ] **Step 5: Commit**

```bash
git add packages/desktop/src/renderer/src/report/data/gameConstants.ts packages/desktop/test/report.gameConstants.test.ts
git commit -m "feat(desktop): class colors/names + spec names (public facts, to be replaced by sub-project 5)"
```

---

### Task 7: 组件测试环境 + ReportHeader + Meters

**Files:**

- Modify: `packages/desktop/package.json`(devDeps: `d3-scale`、`@types/d3-scale`、`jsdom`、`@testing-library/react`;`d3-scale` 放 dependencies)
- Create: `packages/desktop/src/renderer/src/report/components/ReportHeader.tsx`、`Meters.tsx`
- Modify: `packages/desktop/src/renderer/src/styles.css`(追加 report 基础样式)
- Test: `packages/desktop/test/report.components.header.test.tsx`

**Interfaces:**

- Consumes: `deriveRoster`/`RosterTeam`(T2)、`deriveSummary`/`UnitTotals`(T3)、`classColor`/`specName`(T6)
- Produces:

```tsx
export function ReportHeader(props: {
  source: ReportSource;
  roundLabel?: string;
}): JSX.Element;
// 渲染:两队卡片(玩家 name/spec 名/评分,职业色左边框)、中央 结果(Win/Lose/Draw)+时长 mm:ss + bracket + zone <id>;roundLabel 存在时显示(shuffle 回合用)
export function Meters(props: {
  rows: UnitTotals[];
  mode: "damage" | "healing" | "taken";
}): JSX.Element;
// 条形图:每行 名字+数值(千分位)+条(宽度=值/最大值,填充职业色);mode 切换由父组件持有
```

- [ ] **Step 1: 安装依赖**

Run(repo 根): `npm install -w @gladlog/desktop d3-scale && npm install -w @gladlog/desktop -D @types/d3-scale jsdom @testing-library/react`
Expected: 安装成功,`npm ls d3-scale` 显示于 desktop 下

- [ ] **Step 2: 写失败测试**(文件头 `// @vitest-environment jsdom`)

```tsx
// packages/desktop/test/report.components.header.test.tsx
// @vitest-environment jsdom
import { render, screen } from "@testing-library/react";
import { ReportHeader } from "../src/renderer/src/report/components/ReportHeader";
import { Meters } from "../src/renderer/src/report/components/Meters";
import { deriveSummary } from "../src/renderer/src/report/derive/summary";
import { loadMatchFixture } from "./fixtures/loadFixture";

const m = loadMatchFixture();

describe("ReportHeader", () => {
  it("渲染结果、bracket、全部玩家名", () => {
    render(<ReportHeader source={m} />);
    expect(screen.getByText(m.result)).toBeTruthy();
    expect(screen.getByText(new RegExp(m.bracket))).toBeTruthy();
    for (const u of Object.values(m.units)) {
      if (u.kind === "Player" && u.info)
        expect(screen.getByText(u.name)).toBeTruthy();
    }
  });
  it("roundLabel 显示", () => {
    render(<ReportHeader source={m} roundLabel="Round 2" />);
    expect(screen.getByText("Round 2")).toBeTruthy();
  });
});

describe("Meters", () => {
  it("damage 模式:行数=玩家数,首行为最大值且数值千分位出现", () => {
    const rows = deriveSummary(m);
    render(<Meters rows={rows} mode="damage" />);
    const top = rows[0]!;
    expect(screen.getByText(top.name)).toBeTruthy();
    expect(
      screen.getByText(Math.round(top.damageDone).toLocaleString("en-US")),
    ).toBeTruthy();
  });
});
```

- [ ] **Step 3: 确认失败** — Run: `npx vitest run test/report.components.header.test.tsx`,Expected: FAIL(组件缺失)

- [ ] **Step 4: 实现**

```tsx
// src/renderer/src/report/components/ReportHeader.tsx —— 完整文件
import type { ReportSource } from "../derive/types";
import { deriveRoster } from "../derive/roster";
import { classColor, specName } from "../data/gameConstants";

function fmtDuration(ms: number): string {
  const s = Math.max(0, Math.round(ms / 1000));
  return `${Math.floor(s / 60)}:${String(s % 60).padStart(2, "0")}`;
}

export function ReportHeader({
  source,
  roundLabel,
}: {
  source: ReportSource;
  roundLabel?: string;
}) {
  const teams = deriveRoster(source);
  return (
    <header className="rpt-header">
      <div className="rpt-team">
        {teams[0]?.players.map((p) => (
          <div
            key={p.unitId}
            className="rpt-player"
            style={{ borderLeftColor: classColor(p.classId) }}
          >
            <span className="rpt-player-name">{p.name}</span>
            <span className="rpt-player-sub">
              {specName(p.specId)}
              {p.rating !== null ? ` · ${p.rating}` : ""}
            </span>
          </div>
        ))}
      </div>
      <div className="rpt-center">
        {roundLabel ? <div className="rpt-round">{roundLabel}</div> : null}
        <div className={`rpt-result rpt-result-${source.result.toLowerCase()}`}>
          {source.result}
        </div>
        <div className="rpt-meta">
          {source.bracket} · zone {source.zoneId} ·{" "}
          {fmtDuration(source.endTime - source.startTime)}
        </div>
      </div>
      <div className="rpt-team rpt-team-right">
        {teams[1]?.players.map((p) => (
          <div
            key={p.unitId}
            className="rpt-player"
            style={{ borderLeftColor: classColor(p.classId) }}
          >
            <span className="rpt-player-name">{p.name}</span>
            <span className="rpt-player-sub">
              {specName(p.specId)}
              {p.rating !== null ? ` · ${p.rating}` : ""}
            </span>
          </div>
        ))}
      </div>
    </header>
  );
}
```

```tsx
// src/renderer/src/report/components/Meters.tsx —— 完整文件
import type { UnitTotals } from "../derive/summary";
import { classColor } from "../data/gameConstants";

const value = (r: UnitTotals, mode: "damage" | "healing" | "taken"): number =>
  mode === "damage"
    ? r.damageDone
    : mode === "healing"
      ? r.healingDone + r.absorbsDone
      : r.damageTaken;

export function Meters({
  rows,
  mode,
}: {
  rows: UnitTotals[];
  mode: "damage" | "healing" | "taken";
}) {
  const sorted = [...rows].sort((a, b) => value(b, mode) - value(a, mode));
  const max = Math.max(1, ...sorted.map((r) => value(r, mode)));
  return (
    <div className="rpt-meters">
      {sorted.map((r) => {
        const v = value(r, mode);
        return (
          <div
            key={r.unitId}
            className="rpt-meter-row"
            title={`${r.name}: ${Math.round(v).toLocaleString("en-US")}`}
          >
            <span className="rpt-meter-name">{r.name}</span>
            <span className="rpt-meter-bar-track">
              <span
                className="rpt-meter-bar"
                style={{
                  width: `${(v / max) * 100}%`,
                  background: classColor(r.classId),
                }}
              />
            </span>
            <span className="rpt-meter-value">
              {Math.round(v).toLocaleString("en-US")}
            </span>
          </div>
        );
      })}
    </div>
  );
}
```

`styles.css` 追加(基础版,Task 10 打磨):

```css
.rpt-header {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  gap: 16px;
  padding: 12px;
  border: 1px solid #333;
  border-radius: 8px;
}
.rpt-player {
  border-left: 3px solid #666;
  padding: 2px 8px;
  margin: 2px 0;
}
.rpt-player-name {
  display: block;
}
.rpt-player-sub {
  color: #8a8f98;
  font-size: 11px;
}
.rpt-center {
  text-align: center;
  align-self: center;
}
.rpt-result {
  font-size: 22px;
  font-weight: 700;
}
.rpt-result-win {
  color: #4ade80;
}
.rpt-result-lose {
  color: #f87171;
}
.rpt-meta,
.rpt-round {
  color: #8a8f98;
  font-size: 12px;
}
.rpt-meters {
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.rpt-meter-row {
  display: grid;
  grid-template-columns: 140px 1fr 90px;
  align-items: center;
  gap: 8px;
}
.rpt-meter-bar-track {
  background: #1c1f26;
  border-radius: 3px;
  height: 14px;
  overflow: hidden;
}
.rpt-meter-bar {
  display: block;
  height: 100%;
}
.rpt-meter-value {
  text-align: right;
  font-variant-numeric: tabular-nums;
}
```

- [ ] **Step 5: 验证** — Run: `npm test -w @gladlog/desktop && npm run typecheck -w @gladlog/desktop`,Expected: PASS
- [ ] **Step 6: Commit**

```bash
git add packages/desktop/package.json package-lock.json packages/desktop/src/renderer/src/report/components packages/desktop/src/renderer/src/styles.css packages/desktop/test/report.components.header.test.tsx
git commit -m "feat(desktop): report header + meters components with jsdom test setup"
```

---

### Task 8: Timeline 组件(d3-scale + SVG)+ UnitPanel

**Files:**

- Create: `packages/desktop/src/renderer/src/report/components/Timeline.tsx`、`UnitPanel.tsx`
- Modify: `packages/desktop/src/renderer/src/styles.css`(追加)
- Test: `packages/desktop/test/report.components.timeline.test.tsx`

**Interfaces:**

- Consumes: `deriveTimeline`/`TimelineData`(T4)、`deriveCasts`/`deriveAuraEvents`(T5)、`classColor`/`specName`(T6)、`RosterPlayer`(T2)
- Produces:

```tsx
export function Timeline(props: {
  data: TimelineData;
  onSelectUnit?: (unitId: string) => void;
}): JSX.Element;
// SVG:x=scaleLinear([start,end]→[0,W]),y=scaleLinear([0,1]→[H,0]) 绘 hp/maxHp 百分比折线(每 Player 一条,stroke=职业色);
// 死亡标记=倒三角 ▼ 在顶部,title=名字+相对秒;鼠标移动显示竖线游标+相对时间;series 空 → 仅渲染标记轴。
// viewBox="0 0 800 220" + width:100%(响应式);data-testid="rpt-timeline"
export function UnitPanel(props: {
  source: ReportSource;
  unitId: string;
}): JSX.Element;
// 上:玩家名/spec/评分;中:施法序列表(相对秒 mm:ss.s、法术名、目标、宠物标记🐾);下:光环事件表(applied +/-、BUFF/DEBUFF)。行数大时滚动(css)。
```

- [ ] **Step 1: 写失败测试**

```tsx
// packages/desktop/test/report.components.timeline.test.tsx
// @vitest-environment jsdom
import { render, screen } from "@testing-library/react";
import { Timeline } from "../src/renderer/src/report/components/Timeline";
import { UnitPanel } from "../src/renderer/src/report/components/UnitPanel";
import { deriveTimeline } from "../src/renderer/src/report/derive/timeline";
import { deriveCasts } from "../src/renderer/src/report/derive/casts";
import { loadMatchFixture } from "./fixtures/loadFixture";

const m = loadMatchFixture();

describe("Timeline", () => {
  it("每个序列一条 path,死亡标记数量正确", () => {
    const data = deriveTimeline(m);
    const { container } = render(<Timeline data={data} />);
    expect(container.querySelectorAll("path.rpt-tl-line")).toHaveLength(
      data.series.length,
    );
    expect(container.querySelectorAll(".rpt-tl-death")).toHaveLength(
      data.deaths.length,
    );
  });
  it("series 空(无 advanced)也能渲染", () => {
    const data = deriveTimeline({ ...m, hasAdvancedLogging: false });
    const { container } = render(<Timeline data={data} />);
    expect(
      container.querySelector("[data-testid='rpt-timeline']"),
    ).toBeTruthy();
  });
});

describe("UnitPanel", () => {
  it("渲染选中单位的名字与施法行数", () => {
    const u = Object.values(m.units).find(
      (x) => x.kind === "Player" && x.casts.length > 0,
    )!;
    render(<UnitPanel source={m} unitId={u.id} />);
    expect(screen.getAllByText(u.name).length).toBeGreaterThan(0);
    const casts = deriveCasts(m, u.id);
    expect(screen.getByText(new RegExp(`${casts.length}`))).toBeTruthy(); // 面板显示条数
  });
});
```

- [ ] **Step 2: 确认失败** — Run: `npx vitest run test/report.components.timeline.test.tsx`,Expected: FAIL

- [ ] **Step 3: 实现**

```tsx
// src/renderer/src/report/components/Timeline.tsx —— 完整文件
import { scaleLinear } from "d3-scale";
import { useState } from "react";
import type { TimelineData } from "../derive/timeline";
import { classColor } from "../data/gameConstants";

const W = 800,
  H = 220,
  PAD = { l: 34, r: 8, t: 18, b: 18 };

export function Timeline({
  data,
  onSelectUnit,
}: {
  data: TimelineData;
  onSelectUnit?: (unitId: string) => void;
}) {
  const [cursor, setCursor] = useState<number | null>(null);
  const x = scaleLinear()
    .domain([data.start, data.end])
    .range([PAD.l, W - PAD.r]);
  const y = scaleLinear()
    .domain([0, 1])
    .range([H - PAD.b, PAD.t]);
  const relSec = (t: number) => ((t - data.start) / 1000).toFixed(1);

  return (
    <div className="rpt-timeline-wrap">
      <svg
        data-testid="rpt-timeline"
        viewBox={`0 0 ${W} ${H}`}
        className="rpt-timeline"
        onMouseMove={(e) => {
          const rect = e.currentTarget.getBoundingClientRect();
          setCursor(((e.clientX - rect.left) / rect.width) * W);
        }}
        onMouseLeave={() => setCursor(null)}
      >
        {[0, 0.5, 1].map((p) => (
          <g key={p}>
            <line
              x1={PAD.l}
              x2={W - PAD.r}
              y1={y(p)}
              y2={y(p)}
              className="rpt-tl-grid"
            />
            <text x={4} y={y(p) + 4} className="rpt-tl-axis">
              {Math.round(p * 100)}%
            </text>
          </g>
        ))}
        {data.series.map((s) => (
          <path
            key={s.unitId}
            className="rpt-tl-line"
            fill="none"
            stroke={classColor(s.classId)}
            strokeWidth={1.5}
            style={{ cursor: onSelectUnit ? "pointer" : undefined }}
            onClick={() => onSelectUnit?.(s.unitId)}
            d={s.points
              .map(
                (p, i) =>
                  `${i === 0 ? "M" : "L"}${x(p.t).toFixed(1)},${y(p.maxHp > 0 ? p.hp / p.maxHp : 0).toFixed(1)}`,
              )
              .join(" ")}
          >
            <title>{s.name}</title>
          </path>
        ))}
        {data.deaths.map((d, i) => (
          <g
            key={i}
            className="rpt-tl-death"
            transform={`translate(${x(d.t).toFixed(1)},${PAD.t})`}
          >
            <path d="M-5,-10 L5,-10 L0,0 Z" />
            <title>{`${d.name} 死亡 @ ${relSec(d.t)}s`}</title>
          </g>
        ))}
        {cursor !== null && cursor >= PAD.l && cursor <= W - PAD.r ? (
          <g>
            <line
              x1={cursor}
              x2={cursor}
              y1={PAD.t - 12}
              y2={H - PAD.b}
              className="rpt-tl-cursor"
            />
            <text x={cursor + 4} y={PAD.t - 4} className="rpt-tl-axis">
              {relSec(x.invert(cursor))}s
            </text>
          </g>
        ) : null}
      </svg>
    </div>
  );
}
```

```tsx
// src/renderer/src/report/components/UnitPanel.tsx —— 完整文件
import type { ReportSource } from "../derive/types";
import { deriveAuraEvents, deriveCasts } from "../derive/casts";
import { specName } from "../data/gameConstants";

const relTime = (t: number, start: number): string => {
  const s = (t - start) / 1000;
  return `${Math.floor(s / 60)}:${(s % 60).toFixed(1).padStart(4, "0")}`;
};

export function UnitPanel({
  source,
  unitId,
}: {
  source: ReportSource;
  unitId: string;
}) {
  const u = source.units[unitId];
  if (!u) return <div className="rpt-unitpanel">未选中单位</div>;
  const casts = deriveCasts(source, unitId);
  const auras = deriveAuraEvents(source, unitId);
  return (
    <div className="rpt-unitpanel">
      <h3>
        {u.name}{" "}
        <span className="rpt-player-sub">
          {specName(u.specId)}
          {u.info ? ` · ${u.info.personalRating}` : ""}
        </span>
      </h3>
      <h4>施法({casts.length})</h4>
      <div className="rpt-scroll">
        <table>
          <tbody>
            {casts.map((c, i) => (
              <tr key={i}>
                <td className="rpt-t">{relTime(c.t, source.startTime)}</td>
                <td>
                  {c.byPet ? "🐾 " : ""}
                  {c.spellName}
                </td>
                <td className="rpt-dim">{c.targetName}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      <h4>光环事件({auras.length})</h4>
      <div className="rpt-scroll">
        <table>
          <tbody>
            {auras.map((a, i) => (
              <tr key={i}>
                <td className="rpt-t">{relTime(a.t, source.startTime)}</td>
                <td>
                  {a.applied ? "+" : "−"} {a.spellName}
                </td>
                <td className="rpt-dim">{a.auraType}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
}
```

`styles.css` 追加:

```css
.rpt-timeline {
  width: 100%;
  display: block;
  background: #14161b;
  border: 1px solid #333;
  border-radius: 8px;
}
.rpt-tl-grid {
  stroke: #262a33;
  stroke-width: 1;
}
.rpt-tl-axis {
  fill: #6b7280;
  font-size: 10px;
}
.rpt-tl-death path {
  fill: #f87171;
}
.rpt-tl-cursor {
  stroke: #8a8f98;
  stroke-dasharray: 3 3;
}
.rpt-unitpanel h3 {
  margin: 4px 0;
}
.rpt-unitpanel h4 {
  margin: 8px 0 4px;
  color: #8ab4f8;
}
.rpt-scroll {
  max-height: 220px;
  overflow-y: auto;
}
.rpt-scroll table {
  width: 100%;
  border-collapse: collapse;
  font-size: 12px;
}
.rpt-scroll td {
  padding: 1px 6px;
  border-bottom: 1px solid #1c1f26;
}
.rpt-t {
  color: #6b7280;
  font-variant-numeric: tabular-nums;
  white-space: nowrap;
}
.rpt-dim {
  color: #8a8f98;
}
```

- [ ] **Step 4: 验证** — Run: `npm test -w @gladlog/desktop && npm run typecheck -w @gladlog/desktop`,Expected: PASS
- [ ] **Step 5: Commit**

```bash
git add packages/desktop/src/renderer/src/report/components packages/desktop/src/renderer/src/styles.css packages/desktop/test/report.components.timeline.test.tsx
git commit -m "feat(desktop): SVG timeline (d3-scale) + unit detail panel"
```

---

### Task 9: MatchReport/ShuffleReport 组装 + fixtureBridge + App 重构

**Files:**

- Create: `packages/desktop/src/renderer/src/report/components/MatchReport.tsx`、`ShuffleReport.tsx`
- Create: `packages/desktop/src/renderer/src/fixtureBridge.ts`
- Create: `packages/desktop/src/renderer/src/components/DevPanel.tsx`(现调试四栏迁入)
- Rewrite: `packages/desktop/src/renderer/src/App.tsx`
- Modify: `packages/desktop/src/renderer/src/main.tsx`(fixture 模式注入)、`styles.css`
- Test: `packages/desktop/test/report.app.test.tsx`

**Interfaces:**

- Consumes: 全部前序 + `window.gladlog`(`GladlogApi`,preload/api.ts)
- Produces:

```tsx
export function MatchReport(props: {
  source: ReportSource;
  roundLabel?: string;
}): JSX.Element;
// useMemo(derive*) → <ReportHeader/> + meters 模式切换(damage/healing/taken 按钮组)+ <Timeline/> + 右侧 <UnitPanel/>;
// 默认选中 log owner;Timeline onSelectUnit 与 roster 点击切换选中。
export function ShuffleReport(props: { shuffle: StoredShuffle }): JSX.Element;
// 场级头(W/L 序列胶囊,按 rounds[i].winningTeamId===playerTeamId 判)+ 回合 tab(Round 1..n)→ 当前回合 <MatchReport roundLabel/>(仅渲染激活回合 → 派生惰性)。
// fixtureBridge.ts: export function installFixtureBridge(): void
//   构造 mock window.gladlog:matches.list()→[匹配 meta,shuffle meta];get(id)→{schemaVersion:1,kind,data};
//   logs.getStatus→{watching:false,logsDir:"(fixture)",files:[]};事件订阅返回 no-op unsubscribe;settings/app 全 no-op stub。
//   数据源:import matchJson from '../../test/fixtures/report-match.json' + buildSyntheticShuffle 的内联副本(fixtureBridge 不 import test/ 目录 —— 把 buildSyntheticShuffle 逻辑复制为本文件私有函数,注释注明与 loadFixture.ts 保持同步)。
// main.tsx: if (import.meta.env.VITE_FIXTURE_MODE) installFixtureBridge() —— 在 render 之前。
// App.tsx: 左栏对局列表(meta.kind 徽标/bracket/时间/胜负,点击加载)+ 主区(kind==='shuffle'→ShuffleReport else MatchReport)+ 顶栏(标题、开发者视图切换按钮);DevPanel=原调试四栏原样迁移。
```

- [ ] **Step 1: 写失败测试**

```tsx
// packages/desktop/test/report.app.test.tsx
// @vitest-environment jsdom
import { fireEvent, render, screen } from "@testing-library/react";
import { MatchReport } from "../src/renderer/src/report/components/MatchReport";
import { ShuffleReport } from "../src/renderer/src/report/components/ShuffleReport";
import {
  buildSyntheticShuffle,
  loadMatchFixture,
} from "./fixtures/loadFixture";

const m = loadMatchFixture();

describe("MatchReport", () => {
  it("组装:头/meters/时间轴/单位面板齐全,默认选中 log owner", () => {
    const { container } = render(<MatchReport source={m} />);
    expect(screen.getByText(m.result)).toBeTruthy();
    expect(
      container.querySelector("[data-testid='rpt-timeline']"),
    ).toBeTruthy();
    const owner = m.units[m.playerId]!;
    expect(screen.getAllByText(owner.name).length).toBeGreaterThan(1); // header + unit panel
  });
  it("meters 模式切换按钮工作", () => {
    render(<MatchReport source={m} />);
    fireEvent.click(screen.getByRole("button", { name: /治疗/ }));
    expect(
      (screen.getByRole("button", { name: /治疗/ }) as HTMLButtonElement)
        .className,
    ).toMatch(/active/);
  });
});

describe("ShuffleReport", () => {
  it("回合 tab 切换,只渲染激活回合", () => {
    const s = buildSyntheticShuffle(m);
    const { container } = render(<ShuffleReport shuffle={s} />);
    expect(screen.getByText("Round 1")).toBeTruthy();
    expect(
      container.querySelectorAll("[data-testid='rpt-timeline']"),
    ).toHaveLength(1); // 惰性:只有激活回合
    fireEvent.click(screen.getByText("Round 3"));
    expect(
      container.querySelectorAll("[data-testid='rpt-timeline']"),
    ).toHaveLength(1);
  });
});
```

- [ ] **Step 2: 确认失败** — Run: `npx vitest run test/report.app.test.tsx`,Expected: FAIL

- [ ] **Step 3: 实现四个组件 + App 重构**

```tsx
// src/renderer/src/report/components/MatchReport.tsx —— 完整文件
import { useMemo, useState } from "react";
import type { ReportSource } from "../derive/types";
import { deriveSummary } from "../derive/summary";
import { deriveTimeline } from "../derive/timeline";
import { Meters } from "./Meters";
import { ReportHeader } from "./ReportHeader";
import { Timeline } from "./Timeline";
import { UnitPanel } from "./UnitPanel";

type Mode = "damage" | "healing" | "taken";
const MODE_LABEL: Record<Mode, string> = {
  damage: "伤害",
  healing: "治疗",
  taken: "承伤",
};

export function MatchReport({
  source,
  roundLabel,
}: {
  source: ReportSource;
  roundLabel?: string;
}) {
  const [mode, setMode] = useState<Mode>("damage");
  const [unitId, setUnitId] = useState<string>(source.playerId);
  const summary = useMemo(() => deriveSummary(source), [source]);
  const timeline = useMemo(() => deriveTimeline(source), [source]);
  const selected = source.units[unitId]
    ? unitId
    : (summary[0]?.unitId ?? source.playerId);
  return (
    <div className="rpt-match">
      <ReportHeader source={source} roundLabel={roundLabel} />
      <div className="rpt-body">
        <div className="rpt-main">
          <div className="rpt-mode-tabs">
            {(Object.keys(MODE_LABEL) as Mode[]).map((k) => (
              <button
                key={k}
                className={k === mode ? "active" : ""}
                onClick={() => setMode(k)}
              >
                {MODE_LABEL[k]}
              </button>
            ))}
          </div>
          <Meters rows={summary} mode={mode} />
          <Timeline data={timeline} onSelectUnit={setUnitId} />
        </div>
        <aside className="rpt-side">
          <UnitPanel source={source} unitId={selected} />
        </aside>
      </div>
    </div>
  );
}
```

```tsx
// src/renderer/src/report/components/ShuffleReport.tsx —— 完整文件
import { useState } from "react";
import type { StoredShuffle } from "../derive/types";
import { MatchReport } from "./MatchReport";

export function ShuffleReport({ shuffle }: { shuffle: StoredShuffle }) {
  const [active, setActive] = useState(0);
  const round = shuffle.rounds[active] ?? shuffle.rounds[0]!;
  return (
    <div className="rpt-shuffle">
      <div className="rpt-shuffle-head">
        <span className="rpt-shuffle-title">
          Solo Shuffle · {shuffle.rounds.length} 回合 · {shuffle.result}
        </span>
        <span className="rpt-shuffle-seq">
          {shuffle.rounds.map((r, i) => (
            <i
              key={i}
              className={r.winningTeamId === r.playerTeamId ? "w" : "l"}
            >
              {r.winningTeamId === r.playerTeamId ? "W" : "L"}
            </i>
          ))}
        </span>
      </div>
      <div className="rpt-round-tabs">
        {shuffle.rounds.map((_, i) => (
          <button
            key={i}
            className={i === active ? "active" : ""}
            onClick={() => setActive(i)}
          >
            Round {i + 1}
          </button>
        ))}
      </div>
      <MatchReport source={round} roundLabel={`Round ${active + 1}`} />
    </div>
  );
}
```

`fixtureBridge.ts`、`DevPanel.tsx`(把当前 App.tsx 的四栏 JSX+状态原样搬入,导出 `export function DevPanel()`)、`App.tsx` 重写(状态:`metas`、`selectedId`、`doc`(get 结果)、`showDev`;副作用与现版一致的订阅 + `matches.get`;布局:`.app-topbar` + `.app-layout{.app-sidebar list, .app-main}`;`doc.kind==='shuffle'` 时 `data` 需与 `buildSyntheticShuffle` 输出同构——真实 shuffle 的 `data` 即 StoredShuffle)、`main.tsx` 注入。样式追加 `.app-topbar/.app-sidebar/.rpt-body{display:grid;grid-template-columns:1fr 320px}` 等(基础版)。

- [ ] **Step 4: 验证** — Run: `npm test -w @gladlog/desktop && npm run typecheck -w @gladlog/desktop && npm run build -w @gladlog/desktop`,Expected: 全 PASS
- [ ] **Step 5: fixture 模式冒烟**

Run(cwd `packages/desktop`): `VITE_FIXTURE_MODE=1 npx vite dev src/renderer --port 5199` 后台起,`curl -s http://localhost:5199 | grep -c root` = 1;(完整视觉检查在 Task 10 主会话做)——若 electron-vite 的 renderer root 配置使裸 vite 不可用,改跑 `VITE_FIXTURE_MODE=1 npm run dev`(Electron 壳内 fixture 模式同样有效)。

- [ ] **Step 6: Commit**

```bash
git add packages/desktop/src/renderer packages/desktop/test/report.app.test.tsx
git commit -m "feat(desktop): match/shuffle report assembly, fixture bridge, app shell restructure"
```

---

### Task 10: 视觉打磨(主会话执行,非 subagent)

**Files:**

- Modify: `packages/desktop/src/renderer/src/styles.css`(及少量组件 className 调整)

**流程**(这是设计判断型任务,由主会话 Claude 按 frontend-design + dataviz 技能执行;两技能在动手前必读):

- [ ] **Step 1**: 读 frontend-design 与 dataviz 技能;fixture 模式起 dev(`VITE_FIXTURE_MODE=1 npm run dev -w @gladlog/desktop`)
- [ ] **Step 2**: 迭代暗色数据密集视觉:统一色板(背景层次/文字层级/职业色使用规范)、字号节奏、间距网格、meters/时间轴的 dataviz 规范(轴、网格、tooltip 对比度)、shuffle W/L 胶囊
- [ ] **Step 3**: `screencapture` 截图(match 视图、shuffle 视图、单位面板)发用户;**用户确认视觉后才算过**(硬 gate,等待回复)
- [ ] **Step 4**: Commit `style(desktop): report visual polish pass`

---

### Task 11: 壳内端到端 + 收官

**Files:**

- Modify: `.superpowers/progress.md`(gitignored,本地账本)、`README.md`(勾选子项目 3)

- [ ] **Step 1: 真实数据端到端(主会话)**:清 userData matches → `npx electron out/main/index.js` + replay 语料(沿用子项目 2 的 e2e 流程)→ 列表出现对局 → 战报打开:头/meters/时间轴/单位面板/(如有 shuffle)回合导航全部渲染正确;`screencapture` 留档。
- [ ] **Step 2: 全仓验证**:`npm test --workspaces --if-present && npm run typecheck --workspaces --if-present` 全绿;`npm run package:mac -w @gladlog/desktop` 出包安装冒烟一遍战报。
- [ ] **Step 3**: 最终 whole-branch review(SDD 流程,pro 模型)→ findings 修复闭环。
- [ ] **Step 4**: 更新 README 勾选 + 本地账本;Commit `docs: sub-project 3 (combat report UI) complete`。

---

## Self-Review(计划自查记录)

- **Spec 覆盖**:比分头(T7)、meters(T7)、时间轴(T8)、单位详情(T8)、shuffle 导航(T9)、fixture 模式(T9)、App 重构+开发者视图(T9)、暗色原创视觉(T10)、d3-scale(T8)、派生惰性 useMemo(T9)、职业色/spec 名(T6)、脱敏 fixture(T1)、端到端+打包(T11)。spec 全要求有任务。
- **占位符**:Task 1 内自我纠正的顺序调整已落地(loadFixture 归 T2);T9 的 DevPanel/App 属"迁移现有代码+布局组装",给了状态与布局契约而非全文(现 App.tsx 代码在仓库内,implementer 直接读)——不算占位。
- **类型一致性**:`ReportSource`/`StoredMatch`/`StoredShuffle`(T2)贯穿 T3-T9;`UnitTotals`(T3)→T7;`TimelineData`(T4)→T8;`CastRow/AuraRow`(T5)→T8;`classColor/specName`(T6)→T7/T8。已核对。
- **风险**:fixture 日志 `3v3_tww_1120_reduced.txt` 在审计中为上游 blob(DERIVED)——它只作为**解析输入**产出我方数据结构的 JSON,产物是解析结果非上游代码;若仍有顾虑,T1 换用自采语料任意 3v3 日志(`playstyle-logs-cache` 单场缓存,但体积 12MB→JSON 数 MB,需 `--max-bytes` 截断或选更小自采样本)。执行时优先自采小样,reduced 文件为兜底。
