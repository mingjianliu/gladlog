# M5 验收报告:性能基准 + T2 全量浸泡

日期:2026-07-10。

## 性能基准(T1-200,全管线 L1+L2+L3)

- 修复前:22,960 行/秒(瓶颈 = 每行构造 Intl.DateTimeFormat)
- 修复后(per-timezone formatter 缓存,commit 861b3c1):**104,802 行/秒** —— spec 目标 ≥50k 的 2.1 倍,实时 tail 需求的 ~100 倍。
- tsx 与编译产物(esbuild)速度相同,瓶颈为真实计算,已定位并消除。

## T2 全量浸泡(自采语料全集)

| 指标 | 值 |
| --- | --- |
| 文件 | 6,210(104GB) |
| 非空行 | 386,247,153 |
| 耗时 | 3,636s(≈1h),106,222 行/秒 |
| 构建对局 | 3,060 matches + 3,150 shuffles |
| 错误 / 诊断 / 丢行 | **0 / 0 / 0** |

## 子项目 1 收官判定

M1(SNR 3.86 亿行零失败)、M2(切分器 + 六场景契约)、M3(构建器 + 黄金断言)、M4(差分:Level-1 599/600、未裁决差异 0)、M5(性能 2× 目标 + 浸泡零异常)全部达成。**@gladlog/parser 与 @gladlog/parser-compat 可进入子项目 2/4 消费。**

遗留清单(不阻塞):L1 显式剥 \r(裁决 #15);未知事件族按需类型化;classMetadata 注入点等子项目 5 数据。
