# HANDOFF 2026-07-10 — gladlog (complete rewrite of wowarenalogs)

> **Archived 2026-09-26** — historical handoff; paths and state may no longer match the repo.

Full handoff for new sessions. Read this file + `.superpowers/progress.md` to continue seamlessly.

## 1. What the Project Is and Why It Exists

Upstream wowarenalogs changed its license from MIT to CC BY-NC-ND (prohibiting distribution of modified versions) on 2025-12-08 (commit `cf2e72ea`). The user decided to **rewrite from scratch** as gladlog: **zero upstream code used (including the MIT era)**, only reusing the user's own original code assets (~1,239 CLEAN files, per audit).

- This repo: `/Users/mingjianliu/code/gladlog` (orphan history, MIT, no remote; `gh repo create` before release).
- Old fork: `/Users/mingjianliu/code/wowarenalogs` (**to be made private — TODO**; three uses: provenance audit, differential testing private oracle, 104GB self-collected log corpus).

## 2. Hard Constraints (every new session must follow these)

1. **Compliance**: Implementers (agy/subagent) must not read upstream source code in the old fork's `packages/parser/` etc. Allowed inputs: gladlog's specs/plans/test contracts, wowpedia docs, real log samples, runtime observed values (enum manifests and other interop facts). The diff tool lives in the old fork at `scratch/parser-diff/` and does not enter this repo.
2. **Workflow** (user-explicit requirement): Delegate concrete code to agy (`node ~/.claude/skills/agy/scripts/agy-run.mjs exec`); when flash quota runs out → `--model claude-opus` / `--model gpt-oss` → downgrade to haiku subagent. Test contracts written by Claude, implementation output must be spot-checked, green-light independently verified by Claude. TDD, commit per task.
3. **Adjudication principle**: The old parser is not ground truth; all old-vs-new discrepancies are adjudicated against the raw log; when the new side is correct, mark NEW_CORRECT — don't compromise.
4. User prefers continuous autonomous execution ("keep going, don't wait for me"); only pause at genuine user decision points.

## 3. Current Progress (roadmap: 6 sub-projects)

| #   | Sub-project                               | Status                                                                                                                                        |
| --- | ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Compliance audit                          | ✅ (report in old fork `docs/analysis/2026-07-10-own-code-audit*.md`: 1239 CLEAN / 16 NEEDS_SCRUB (with line numbers) / 2 DERIVED + 89-file hunk appendix) |
| 1   | Parser library                            | ✅ Completed this session, see below                                                                                                           |
| 2   | Desktop shell (Electron+Vite)             | ⬅️ **Up next** (brainstorm+spec first)                                                                                                        |
| 3   | Match report UI (visuals must be original)| Not started                                                                                                                                   |
| 4   | AI analysis + eval migration              | Not started (depends on compat; includes "data realignment phase")                                                                             |
| 5   | Game data pipeline (incl. classMetadata)  | Not started                                                                                                                                   |

## 4. Sub-project 1 Deliverables (this repo)

- `packages/parser` (`@gladlog/parser`): L1 line parser (`src/l1/`) → L2 segmenter (`src/l2/`) → L3 builder (`src/l3/`, GladMatch/GladShuffle); public API `GladLogParser` (push/end/on('match'|'shuffle'|'diagnostic')/stats). Zero runtime dependencies, browser-compatible.
- `packages/parser-compat` (`@gladlog/parser-compat`): legacy shape adapter — enums pinned to runtime manifest (`data/legacy-enum-manifest.json`; note: legacy Class enum order ≠ Blizzard official order), `toLegacyMatch/toLegacyShuffle`, `WoWCombatLogParser` drop-in entry point (covers all 7 call sites in old code).
- Acceptance reports: `docs/reports/2026-07-10-{m1-snr-acceptance,m4-differential-report,m5-perf-t2}.md`.
- Key numbers: SNR 386M lines 0 failures; differential Level-1 599/600 (remaining case = old parser was wrong); throughput 106k lines/sec; T2 soak test 104GB all green.
- ~150 tests; fixture set requires `GLADLOG_FIXTURES=/Users/mingjianliu/code/wowarenalogs/packages/parser/test/testlogs` (auto-skipped if unset).

**Compat-replicated legacy pipeline conventions** (must read before modifying convert.ts; details in M4 report adjudication ledger + old fork `scratch/parser-diff/adjudications.md`, all 30 entries): damage amount/effective negated, effective=amount−overkill−absorbed; SPELL_ABSORBED mixed into **attacker's damageOut** as positive numbers (not mixed into damageIn); pet/guardian dmg+heal merged into owner, pet target rows eff=0; SPELL_SUMMON establishes totem ownership; CI-less Player filtering (outsider); spellId/teamId family all strings, spellSchoolId='0x..'; logLine.parameters raw params (numeric coerce); CombatantInfo shape (talents={id1,id2,count}[] etc.); advancedActions carry logLine.timestamp+advancedActorId.

**Known leftovers (non-blocking)**: L1 explicitly strips \\r; unknown event families (_MISSED/_SUPPORT/DAMAGE_SPLIT etc., zero downstream references) to be typed on demand; shuffle round double-death 0.75s edge case outcome = new side's determination (old was wrong).

## 5. Next Step: How to Start Sub-project 2 (Desktop Shell)

1. New session: read this file + `.superpowers/progress.md` + roadmap spec (`docs/specs/2026-07-10-clean-rewrite-roadmap-design.md`).
2. Brainstorm → spec (save to `docs/specs/`, run one round of agy debate per convention and record it). Key reconnaissance points (old fork, read-only on user's own changes, not upstream source; architectural facts may reference old fork's CLAUDE.md): log directory watch semantics (WoWCombatLog rotation / multi-version directories), which downstream user code consumes the `window.wowarenalogs` preload bridge (check audit hunk appendix), the local Next server architecture is user-owned (f5a6c299) but this project uses Vite — loading approach needs redesign.
3. After user reviews spec: writing-plans → SDD execution (agy-first).

## 6. Common Commands and Pitfalls

- Tests: `npm test --workspaces --if-present`; typecheck likewise. Fixture set: see environment variable above.
- Differential re-run (old fork root): `npx tsx scratch/parser-diff/runContextDiff.ts --manifest scratch/parser-diff/t1-manifest.txt` (Level-2); `./scratch/parser-diff/runLevel1.sh` (Level-1); output in /tmp.
- Performance benchmark: `node_modules/.bin/tsx packages/parser/scripts/benchPipeline.ts --manifest <absolute-path-manifest>`.
- **Pitfalls**: shell cwd drifts between the two repos (always use absolute paths); zsh doesn't word-split unquoted variables (use xargs or eval); long-running batches need `caffeinate -i` to prevent sleep (hit this twice); all commits in the old fork trigger slow hooks (don't commit diff artifacts there — they're gitignored anyway); tsx is strict about type-only imports (`import type`).
- Quota: re-session Fable orchestration is expensive; mechanical milestones can use cheaper primary model sessions (user is aware).

## 7. Branding and Release Reminder

The new project's public name is gladlog; do not use the "WoW Arena Logs" name/logo. README already has MIT LICENSE (user's copyright). Audit reports can be made public when needed as provenance proof.
