# HANDOFF 2026-07-10 — gladlog(wowarenalogs 彻底重写)

给新会话的完整交接。读完本文件 + `.superpowers/progress.md` 即可无缝接续。

## 一、项目是什么、为什么存在

上游 wowarenalogs 于 2025-12-08(commit `cf2e72ea`)把许可证从 MIT 改为 CC BY-NC-ND(禁止分发修改版)。用户决定**彻底从零重写**自己的版本 gladlog:**一行上游代码不用(含 MIT 期)**,只复用自己原创的代码资产(~1,239 个 CLEAN 文件,见审计)。

- 本仓库:`/Users/mingjianliu/code/gladlog`(orphan 历史,MIT,无远端;发布前 `gh repo create`)。
- 旧 fork:`/Users/mingjianliu/code/wowarenalogs`(**转 private 待办**;三个用途:审计出处、差分测试私有 oracle、104GB 自采日志语料)。

## 二、硬性约束(每个新会话都必须遵守)

1. **合规**:实现者(agy/subagent)不得阅读旧 fork 的 `packages/parser/` 等上游源码。允许输入:gladlog 的 spec/计划/测试契约、wowpedia 文档、真实日志样本、运行时观测值(枚举 manifest 等互操作事实)。差分工具驻旧 fork `scratch/parser-diff/`,不进本仓库。
2. **工作方式**(用户明确要求):具体代码优先派 agy(`node ~/.claude/skills/agy/scripts/agy-run.mjs exec`);flash 额度尽 → `--model claude-opus` / `--model gpt-oss` → 降级 haiku subagent。测试契约由 Claude 写、实现方产出必须抽查、绿灯由 Claude 独立验证。TDD、逐任务 commit。
3. **裁决原则**:旧 parser 不是真理;所有新旧分歧按原始日志仲裁;新侧正确记 NEW_CORRECT 不迁就。
4. 用户偏好连续自主执行("一直做 不用等我"),只在真正的用户决策点停。

## 三、当前进度(路线图 6 个子项目)

| #   | 子项目                              | 状态                                                                                                                               |
| --- | ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 0   | 合规审计                            | ✅(报告在旧 fork `docs/analysis/2026-07-10-own-code-audit*.md`:1239 CLEAN / 16 NEEDS_SCRUB(带行号)/ 2 DERIVED + 89 文件 hunk 附录) |
| 1   | Parser 库                           | ✅ 本次完成,见下                                                                                                                   |
| 2   | 桌面壳(Electron+Vite)               | ⬅️ **下一个**(先 brainstorm+spec)                                                                                                  |
| 3   | 战报 UI(视觉必须原创)               | 未开始                                                                                                                             |
| 4   | AI 分析+eval 移植                   | 未开始(依赖 compat;含"数据再对齐期")                                                                                               |
| 5   | 游戏数据管线(含 classMetadata 自建) | 未开始                                                                                                                             |

## 四、子项目 1 交付物(本仓库)

- `packages/parser`(`@gladlog/parser`):L1 行解析(`src/l1/`)→ L2 切分器(`src/l2/`)→ L3 构建器(`src/l3/`,GladMatch/GladShuffle);公共 API `GladLogParser`(push/end/on('match'|'shuffle'|'diagnostic')/stats)。零运行时依赖,浏览器可用。
- `packages/parser-compat`(`@gladlog/parser-compat`):旧形状适配——枚举钉死运行时 manifest(`data/legacy-enum-manifest.json`,注意旧 Class 枚举序≠暴雪官方序)、`toLegacyMatch/toLegacyShuffle`、`WoWCombatLogParser` 仿形入口(覆盖旧代码全部 7 个调用点)。
- 验收报告:`docs/reports/2026-07-10-{m1-snr-acceptance,m4-differential-report,m5-perf-t2}.md`。
- 关键数字:SNR 3.86 亿行 0 失败;差分 Level-1 599/600(残例=旧判错);吞吐 106k 行/秒;T2 浸泡 104GB 全绿。
- 测试 ~150 个;fixture 组需 `GLADLOG_FIXTURES=/Users/mingjianliu/code/wowarenalogs/packages/parser/test/testlogs`(无变量自动 skip)。

**compat 复刻的旧管线惯例**(动 convert.ts 前必读,详见 M4 报告裁决台账 + 旧 fork `scratch/parser-diff/adjudications.md` 全 30 条):伤害 amount/effective 取负、effective=amount−overkill−absorbed;SPELL_ABSORBED 以正数混编**攻击方 damageOut**(damageIn 不混);宠物/守卫 dmg+heal 并入主人、宠物目标行 eff=0;SPELL_SUMMON 建立图腾归属;CI-less Player 过滤(outsider);spellId/teamId 族全字符串、spellSchoolId='0x..';logLine.parameters 原始参数(数值 coerce);CombatantInfo 形状(talents={id1,id2,count}[] 等);advancedActions 带 logLine.timestamp+advancedActorId。

**已知遗留(不阻塞)**:L1 显式剥 \r;未知事件族(_MISSED/_SUPPORT/DAMAGE_SPLIT 等,下游零引用)按需类型化;shuffle round 双死亡 0.75s 案例类的胜负=新侧口径(旧错)。

## 五、下一步:子项目 2(桌面壳)怎么开工

1. 新会话先读本文件 + `.superpowers/progress.md` + roadmap spec(`docs/specs/2026-07-10-clean-rewrite-roadmap-design.md`)。
2. brainstorm→spec(存 `docs/specs/`,按惯例跑一轮 agy debate 并记录)。关键侦察点(旧 fork,只读用户自有改动而非上游源码;架构事实可参考旧 fork CLAUDE.md):日志目录监控语义(WoWCombatLog 轮转/多版本目录)、`window.wowarenalogs` preload bridge 由用户下游哪些代码消费(审计 hunk 附录可查)、本地 Next server 架构是用户自有(f5a6c299)但本项目用 Vite,需重设计加载方式。
3. spec 用户审阅后 writing-plans → SDD 执行(agy-first)。

## 六、常用命令与坑

- 测试:`npm test --workspaces --if-present`;typecheck 同理。fixture 组见上方环境变量。
- 差分复跑(旧 fork 根目录):`npx tsx scratch/parser-diff/runContextDiff.ts --manifest scratch/parser-diff/t1-manifest.txt`(Level-2);`./scratch/parser-diff/runLevel1.sh`(Level-1);产物在 /tmp。
- 性能基准:`node_modules/.bin/tsx packages/parser/scripts/benchPipeline.ts --manifest <绝对路径清单>`。
- **坑**:shell cwd 在两仓库间漂移(始终用绝对路径);zsh 不分词未引号变量(xargs 或 eval);长跑批用 `caffeinate -i` 防休眠(踩过两次);旧 fork 一切 commit 会触发慢钩子(别在旧 fork commit 差分产物,它们本来就 gitignored);tsx 对 type-only import 严格(`import type`)。
- 额度:重会话 Fable 编排贵;机械里程碑可用便宜主模型会话跑(用户知情)。

## 七、品牌与发布提醒

新项目对外名 gladlog,不使用 "WoW Arena Logs" 名称/logo;README 已有 MIT LICENSE(用户版权)。审计报告可在需要时公开作为出处证明。
